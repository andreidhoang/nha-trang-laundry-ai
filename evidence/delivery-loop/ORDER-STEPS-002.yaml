schema_version: 1
work_item: ORDER-STEPS-002
recorded_at: '2026-09-26T01:00:00+07:00'
requirement_contract: REWASH (production QUALITY_CHECK or READY_AT_STORE on an ACTIVE, not-yet-collected
  order -> EXCEPTION -> IN_PROCESS) and REJECT_INTAKE (a received, pre-acceptance intake before ACTIVE
  -> REJECTED, commercial -> CANCELLED) are composite steps planned by the real transition functions,
  each with a strict reason enum recorded on the first transition's event and audit; neither is ever primary.
rollback_impact: Additive enums and request fields; no migration; the per-axis routes are unchanged. Revert
  the merge. Rolling data back is docs/runbooks/restore-drill.md from the base backup shop-till-mac.md
  section 8 takes before every upgrade.
unresolved_assumptions: A walk-in bag before Nhận đồ is at AWAITING_HANDOFF, where REJECT_INTAKE is refused
  by the domain (custody never received); the way out there is Huỷ đơn, with no reason recorded.
evidence:
  the_steps_are_planned_by_the_real_transition_functions: 'packages/domain/tests/test_order_steps.py (159
    passed in the slice run): REWASH over every production x commercial state, REJECT_INTAKE over every
    intake x commercial state, reason only on the first transition, no rewash after collection.'
  a_step_records_its_reason_on_the_event_and_audit: 'test_order_step_repository.py: two audited transitions
    with the reason on the first; replay; IDEMPOTENCY_CONFLICT on a changed reason; a pre-change RECEIVE
    key still hashes the same; the history names step and reason.'
  neither_step_is_ever_the_primary_step: NEVER_PRIMARY_STEPS in the domain; a list whose only legal steps
    are never-primary has no primary; stub suite shows Chưa có bước tiếp theo beside Khác.
  walked_on_the_real_api_at_phone_size: 'filmed at phone size (rework): 26 ok, 0 failed'
  rollback_assessment: Additive enums and request fields; no migration; the per-axis routes are unchanged.
    Revert the merge. Rolling data back is docs/runbooks/restore-drill.md from the base backup shop-till-mac.md
    section 8 takes before every upgrade.
checks:
- command: uv run ruff check .
  status: PASSED
  result: All checks passed!
- command: uv run ruff format --check .
  status: PASSED
  result: 657 files already formatted
- command: uv run mypy apps packages
  status: PASSED
  result: 'Success: no issues found in 325 source files'
- command: uv run pytest --require-postgres-integration
  status: PASSED
  result: 2679 passed, 5 skipped, 1 warning in 839.54s (0:13:59)
- command: uv run python scripts/verify_contracts.py
  status: PASSED
  result: Validated 669 synthetic combinatorial cases against the domain engines. Validated 77 served
    internal API operations against their contract. Validated 475 staff-console disclosures against their
    registry. Validated 16 pinned public-runtime artifacts; release blockers=10.
- command: uv run python scripts/check_context_drift.py
  status: PASSED
  result: 'Context drift check passed: 161 source references, 39 decisions, 4 gates, 9 phases, 182 work
    items, 13 capabilities, 5 decision-gated items.'
supplementary_checks:
- command: verify_console_interaction.py (stubbed API)
  status: PASSED
  result: 433 ok, 0 failed
- command: verify_daily_operations.py, real API, desk size, database created empty
  status: PASSED
  result: 78 ok, 0 failed
- command: verify_workflow_conformance.py, whole run, real API, desk size
  status: PASSED
  result: 317 ok, 0 failed, 96/96 declared controls
- command: verify_daily_operations.py, real API, phone size (CONSOLE_VIEWPORT=phone)
  status: PASSED
  result: 78 ok, 0 failed
- command: verify_workflow_conformance.py, whole run, real API, phone size
  status: PASSED
  result: 317 ok, 0 failed, 96/96 declared controls
