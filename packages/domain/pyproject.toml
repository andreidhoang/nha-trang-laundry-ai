[project]
name = "nha-trang-laundry-domain"
version = "0.1.0"
description = "Pure deterministic money, state, policy-input, delivery, and SLA rules."
requires-python = ">=3.12,<3.14"
dependencies = [
  "rfc8785>=0.1.4,<1",
]

[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"
