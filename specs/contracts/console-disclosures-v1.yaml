schema_version: 1
purpose: 'The staff console''s honesty-chrome disclosures, with what holds each one true. Coverage is
  stated precisely rather than absolutely, because the first version of this registry claimed to hold
  ''every'' disclosure while its enumerator could only see six object keys and the first paragraph of
  a notice - a claim that was true of the enumerator, not of the console. It now enumerates those keys,
  strings rendered with a props object under the claim classes, notice bodies, and the MESSAGES and REASON_NOTE
  tables in core/. A string rendered some other way is still outside it. Generated by scripts/generate_console_disclosure_registry.py
  and verified by scripts/verify_contracts.py against a fresh generation, so a new disclosure cannot land
  unregistered and a reworded one cannot pass unnoticed: a slot''s identity includes the hash of its text.'
counts:
  ABSENT_ROUTE: 2
  ABSENT_TABLE: 5
  ALL_AUTHENTICATED: 1
  DESCRIPTIVE: 135
  MODEL_SEAM: 2
  POLICY_BOUND: 1
  READ_ONLY_MODULE: 1
  REPOSITORY_ROLES: 3
  RESPONSE_SHAPE: 1
  SERVER_GATE: 11
total: 162
disclosures:
- slot_id: core/errors.js#MESSAGES.OFFLINE:d1fe6e497f69
  module: core/errors.js
  key: MESSAGES.OFFLINE
  text: Mất kết nối mạng. Thao tác không được xếp hàng và không có gì được gửi đi.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: core/errors.js#MESSAGES.NETWORK:90de1c4b7c3d
  module: core/errors.js
  key: MESSAGES.NETWORK
  text: Không gọi được máy chủ. Không rõ lệnh đã tới nơi hay chưa.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: core/errors.js#MESSAGES.TIMEOUT:fd4b19f9cddc
  module: core/errors.js
  key: MESSAGES.TIMEOUT
  text: Máy chủ không trả lời kịp. Không rõ lệnh đã tới nơi hay chưa.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: core/errors.js#MESSAGES.SESSION_ENDED:db85538f50e1
  module: core/errors.js
  key: MESSAGES.SESSION_ENDED
  text: Phiên đăng nhập đã kết thúc. Hãy đăng nhập lại.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: core/errors.js#MESSAGES.STALE:6e0156a13dcf
  module: core/errors.js
  key: MESSAGES.STALE
  text: Dữ liệu đã thay đổi từ khi bạn mở màn hình. Hãy tải lại rồi làm lại.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: core/errors.js#MESSAGES.IDEMPOTENCY_CONFLICT:aee66571f488
  module: core/errors.js
  key: MESSAGES.IDEMPOTENCY_CONFLICT
  text: Cùng một khoá thao tác đã dùng cho nội dung khác. Hãy tải lại rồi nhập lại.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: core/errors.js#MESSAGES.REQUIRE_HUMAN:1100d3835506
  module: core/errors.js
  key: MESSAGES.REQUIRE_HUMAN
  text: Cần người quyết định. Máy chủ không tự chọn.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: core/errors.js#MESSAGES.PRECONDITION_REQUIRED:cc547ef3e1a0
  module: core/errors.js
  key: MESSAGES.PRECONDITION_REQUIRED
  text: Thiếu phiên bản dòng dữ liệu. Hãy tải lại màn hình.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: core/errors.js#MESSAGES.PRICEBOOK_UNAVAILABLE:25c14482ae8e
  module: core/errors.js
  key: MESSAGES.PRICEBOOK_UNAVAILABLE
  text: Chưa có bảng giá được duyệt cho cửa hàng này. Không có bảng giá thì không có giá.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: core/errors.js#MESSAGES.FAULT:42fee09ef5d3
  module: core/errors.js
  key: MESSAGES.FAULT
  text: Máy chủ gặp lỗi. Đừng thử lại — hãy kiểm tra bảng đơn để xem lệnh đã vào hay chưa.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: core/i18n.js#REASON_NOTE.TAX_TREATMENT_UNVERIFIED:13c50faa5a93
  module: core/i18n.js
  key: REASON_NOTE.TAX_TREATMENT_UNVERIFIED
  text: Mọi báo giá R1 đều mang mã này. Số tiền hiển thị không được gọi là đã gồm hay chưa gồm thuế.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: core/i18n.js#REASON_NOTE.DELIVERY_FEE_UNRESOLVED:1016a2f49766
  module: core/i18n.js
  key: REASON_NOTE.DELIVERY_FEE_UNRESOLVED
  text: Chưa có phí giao hàng nên chưa có tổng cuối. Tiền dịch vụ bên dưới không phải số khách trả.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: core/i18n.js#REASON_NOTE.PROMOTION_NOT_EVALUATED:4a41314ec41c
  module: core/i18n.js
  key: REASON_NOTE.PROMOTION_NOT_EVALUATED
  text: Chưa xét khuyến mãi cho bản báo giá này.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: core/i18n.js#REASON_NOTE.MISSING_REQUIRED_FACT:d61bbf747959
  module: core/i18n.js
  key: REASON_NOTE.MISSING_REQUIRED_FACT
  text: Thiếu dữ kiện bắt buộc; máy chủ không đoán.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: core/i18n.js#REASON_NOTE.RANGE_PRICE_REQUIRES_HUMAN:7148601fd1d6
  module: core/i18n.js
  key: REASON_NOTE.RANGE_PRICE_REQUIRES_HUMAN
  text: Dịch vụ này có khoảng giá; nhân viên phải chọn giá chính xác.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: core/i18n.js#REASON_NOTE.PRICE_RULE_UNRESOLVED:7218f5fc4bdc
  module: core/i18n.js
  key: REASON_NOTE.PRICE_RULE_UNRESOLVED
  text: Không có quy tắc giá áp dụng được cho dòng này.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: core/i18n.js#REASON_NOTE.MEASUREMENT_POLICY_UNRESOLVED:8e2e66ae083c
  module: core/i18n.js
  key: REASON_NOTE.MEASUREMENT_POLICY_UNRESOLVED
  text: Chưa có chính sách đo lường được công bố cho cơ sở khối lượng này.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: core/i18n.js#REASON_NOTE.VALIDATION_ERROR:185e6975e368
  module: core/i18n.js
  key: REASON_NOTE.VALIDATION_ERROR
  text: Dữ liệu vào không hợp lệ với quy tắc miền.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: core/i18n.js#REASON_NOTE.CONTACT_BINDING_UNKNOWN:8543400ac002
  module: core/i18n.js
  key: REASON_NOTE.CONTACT_BINDING_UNKNOWN
  text: Không có liên hệ nào mang mã này. Liên hệ chỉ được tạo từ một hội thoại kênh đã xác minh; màn
    hình tiếp nhận không tạo liên hệ mới.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: core/rbac.js#why:f1857f4fe905
  module: core/rbac.js
  key: why
  text: Bất kỳ phiên hợp lệ nào cũng đọc được danh sách cửa hàng của chính mình.
  binding:
    kind: ALL_AUTHENTICATED
    capability: STORES_READ
    why: The claim is that any valid session may do this. Asserted as equality with the full StaffRole
      enumeration, so it fails if the console drops a role and also if a new role is added and the console
      is not updated.
- slot_id: core/rbac.js#why:5a6260e93bad
  module: core/rbac.js
  key: why
  text: Kho dữ liệu đơn hàng cho phép bốn vai trò này đọc.
  binding:
    kind: REPOSITORY_ROLES
    capability: ORDERS_READ
    module: nha_trang_laundry_db.orders
    symbol: _require_order_read
    why: The route gate for this capability admits every role, so binding to it could never fail. The
      claim is really enforced by an exact role set in the repository layer, and the test asserts the
      console's list equals it.
- slot_id: core/rbac.js#why:e64ecdbabe78
  module: core/rbac.js
  key: why
  text: Tạo và chuyển trạng thái đơn cần vai trò vận hành và đã xác thực hai bước.
  binding:
    kind: SERVER_GATE
    capability: ORDERS_WRITE
    gate: require_operations_staff
    why: The console declares which roles and MFA state this capability needs. The test probes the named
      server gate and requires the console to be no more permissive than it is.
- slot_id: core/rbac.js#why:995cc17e3773
  module: core/rbac.js
  key: why
  text: Danh sách báo giá dùng cổng vận hành; AUDITOR bị từ chối ở đây dù đọc được đơn hàng.
  binding:
    kind: SERVER_GATE
    capability: QUOTES_READ
    gate: require_operations_staff
    why: The console declares which roles and MFA state this capability needs. The test probes the named
      server gate and requires the console to be no more permissive than it is.
- slot_id: core/rbac.js#why:129b1f2f9218
  module: core/rbac.js
  key: why
  text: Tính giá cần vai trò vận hành và đã xác thực hai bước.
  binding:
    kind: SERVER_GATE
    capability: QUOTES_WRITE
    gate: require_operations_staff
    why: The console declares which roles and MFA state this capability needs. The test probes the named
      server gate and requires the console to be no more permissive than it is.
- slot_id: core/rbac.js#why:b866fb76adad
  module: core/rbac.js
  key: why
  text: Tiền đã thu hôm nay dùng cùng cổng vận hành với báo giá.
  binding:
    kind: SERVER_GATE
    capability: SETTLEMENTS_READ
    gate: require_operations_staff
    why: The console declares which roles and MFA state this capability needs. The test probes the named
      server gate and requires the console to be no more permissive than it is.
- slot_id: core/rbac.js#why:e511e6509b25
  module: core/rbac.js
  key: why
  text: Sự cố dùng cổng vận hành.
  binding:
    kind: SERVER_GATE
    capability: INCIDENTS_READ
    gate: require_operations_staff
    why: The console declares which roles and MFA state this capability needs. The test probes the named
      server gate and requires the console to be no more permissive than it is.
- slot_id: core/rbac.js#why:ce7a54293201
  module: core/rbac.js
  key: why
  text: Mở sự cố cần vai trò vận hành và đã xác thực hai bước.
  binding:
    kind: SERVER_GATE
    capability: INCIDENTS_WRITE
    gate: require_operations_staff
    why: The console declares which roles and MFA state this capability needs. The test probes the named
      server gate and requires the console to be no more permissive than it is.
- slot_id: core/rbac.js#why:31cf6ac1ce23
  module: core/rbac.js
  key: why
  text: Hàng chờ duyệt chỉ dành cho người có quyền duyệt.
  binding:
    kind: SERVER_GATE
    capability: APPROVALS_READ
    gate: require_approval_staff
    why: The console declares which roles and MFA state this capability needs. The test probes the named
      server gate and requires the console to be no more permissive than it is.
- slot_id: core/rbac.js#why:13a623d045d0
  module: core/rbac.js
  key: why
  text: Tình trạng hàng đợi dùng cùng cổng với hàng chờ duyệt.
  binding:
    kind: SERVER_GATE
    capability: QUEUE_READ
    gate: require_approval_staff
    why: The console declares which roles and MFA state this capability needs. The test probes the named
      server gate and requires the console to be no more permissive than it is.
- slot_id: core/rbac.js#why:f0bdf0cf51e3
  module: core/rbac.js
  key: why
  text: Đọc bản nháp, ngoại lệ và dòng thời gian kiểm toán mở cho bốn vai trò.
  binding:
    kind: REPOSITORY_ROLES
    capability: SHADOW_READ
    module: nha_trang_laundry_db.shadow_console
    symbol: SHADOW_READ_ROLES
    why: The route gate for this capability admits every role, so binding to it could never fail. The
      claim is really enforced by an exact role set in the repository layer, and the test asserts the
      console's list equals it.
- slot_id: core/rbac.js#why:8326f683066c
  module: core/rbac.js
  key: why
  text: Quyết định bản nháp và đối soát chỉ dành cho chủ hoặc người duyệt — OPERATOR qua được cổng truy
    cập nhưng bị từ chối ở tầng dữ liệu.
  binding:
    kind: REPOSITORY_ROLES
    capability: SHADOW_DECIDE
    module: nha_trang_laundry_db.shadow_console
    symbol: SHADOW_DECIDE_ROLES
    why: The route gate for this capability admits every role, so binding to it could never fail. The
      claim is really enforced by an exact role set in the repository layer, and the test asserts the
      console's list equals it.
- slot_id: core/rbac.js#why:8554c8780029
  module: core/rbac.js
  key: why
  text: Khoá và chứng thực gửi thủ công cần vai trò vận hành và đã xác thực hai bước.
  binding:
    kind: SERVER_GATE
    capability: MANUAL_SEND
    gate: require_operations_staff
    why: The console declares which roles and MFA state this capability needs. The test probes the named
      server gate and requires the console to be no more permissive than it is.
- slot_id: core/rbac.js#why:3d864b158f0c
  module: core/rbac.js
  key: why
  text: Quản lý nhân sự chỉ dành cho chủ.
  binding:
    kind: SERVER_GATE
    capability: STAFF_ADMIN
    gate: require_owner
    why: The console declares which roles and MFA state this capability needs. The test probes the named
      server gate and requires the console to be no more permissive than it is.
- slot_id: core/rbac.js#why:c2476af7e68f
  module: core/rbac.js
  key: why
  text: 'Trợ lý AI dùng cổng vận hành: vai trò vận hành, đã xác thực hai bước, và được gán cửa hàng.'
  binding:
    kind: SERVER_GATE
    capability: ASSISTANT
    gate: require_operations_staff
    why: The console declares which roles and MFA state this capability needs. The test probes the named
      server gate and requires the console to be no more permissive than it is.
- slot_id: screens/approvals.js#hint:b0c7ac83b054
  module: screens/approvals.js
  key: hint
  text: 'Không bấm được: quyết định cần ba giá trị mà danh sách này không trả về. Xem “Tại sao nút Duyệt
    đang tắt?” trong bảng giới hạn bên dưới.'
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/approvals.js#notice:749274203f80
  module: screens/approvals.js
  key: notice
  text: Lệnh này đã chạy trước đó — đây là bản ghi cũ hiện lại.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/approvals.js#notice:53e222b57566
  module: screens/approvals.js
  key: notice
  text: Máy chủ tìm hàng chờ theo đơn hàng, nên việc chờ duyệt nào không gắn với một đơn — một bản báo
    giá, một tin nhắn soạn sẵn, một khung giờ hay một mức phí giao đề xuất — sẽ không hiện ở đây. Chúng
    vẫn đang chờ và vẫn đang đếm ngược; chỉ là danh sách này không thấy chúng.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/approvals.js#guardrail:2807c7bbd43b
  module: screens/approvals.js
  key: guardrail
  text: Đây là màn hình chỉ đọc. Không có thao tác nào ở đây ghi vào máy chủ, kể cả khi một việc sắp hết
    hạn.
  binding:
    kind: READ_ONLY_MODULE
    module: screens/approvals.js
    why: Claims the screen writes nothing to the server. Checked by scanning the module for a mutating
      request; it issues one GET and nothing else.
- slot_id: screens/approvals.js#hint:c85fe854cb27
  module: screens/approvals.js
  key: hint
  text: 'Hệ quả: danh sách trống không có nghĩa là không còn gì chờ duyệt.'
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/approvals.js#notice:2d352ae01c8f
  module: screens/approvals.js
  key: notice
  text: Việc đã duyệt, đã từ chối hoặc đã hết hạn không nằm trong danh sách này và không tra cứu lại được
    từ đây.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/approvals.js#notice:2ebfcb11400a
  module: screens/approvals.js
  key: notice
  text: Để duyệt, máy chủ đòi ba mã niêm phong chứng minh bạn đã xem đúng nội dung đó. Danh sách trên
    không trả về mã nào trong ba mã ấy — nó chỉ có mã của chính phong bì, là một giá trị khác và không
    thay được. Nên từ những gì màn hình này thấy, không dựng nổi một quyết định hợp lệ.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/approvals.js#notice:85921adfdc91
  module: screens/approvals.js
  key: notice
  text: 'Cùng một lý do: mở một yêu cầu duyệt cần đúng loại việc theo bảng của máy chủ, cộng hai mã niêm
    phong và phiên bản chính sách đang áp dụng. Không giá trị nào trong số đó nhân viên gõ tay ra được,
    nên màn hình không mời bạn thử.'
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/approvals.js#screen__lede:eaeda53c3add
  module: screens/approvals.js
  key: screen__lede
  text: Việc đang chờ duyệt và còn bao lâu nữa hết hạn. Chưa quyết được từ màn hình này; phần bên dưới
    nói rõ vì sao.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/assistant.js#hint:70a54c570c37
  module: screens/assistant.js
  key: hint
  text: Chưa có câu hỏi nào được ghi cho cửa hàng này. Bắt đầu bằng một trong những câu dưới, hoặc tự
    viết câu hỏi của bạn.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/assistant.js#notice:a9d4b5d99812
  module: screens/assistant.js
  key: notice
  text: Viết câu hỏi rồi bấm Gửi. Không có gì được ghi lại.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/assistant.js#screen__lede:96537d9c63dd
  module: screens/assistant.js
  key: screen__lede
  text: Hỏi về tình hình cửa hàng bằng tiếng Việt. Mọi câu trả lời chỉ đến từ dữ liệu vận hành hệ thống
    đang quản lý — điều gì nó không biết, nó nói là không biết, và không bao giờ đoán một con số.
  binding:
    kind: MODEL_SEAM
    service: AssistantService
    default_brain: DeterministicAssistantBrain
    why: The second model-seam claim in the same file, invisible to the first enumerator because it renders
      as a screen__lede. It says every answer comes only from governed operational data and that what
      the assistant does not know, it says it does not know. The same one-line brain swap falsifies it.
- slot_id: screens/assistant.js#notice:383d2d025f90
  module: screens/assistant.js
  key: notice
  text: Trợ lý này không gọi mô hình ngôn ngữ và không tính tiền. Mỗi câu trả lời được máy chủ quyết định
    và ghi lại trước, rồi hiện dần trên màn hình — chữ chạy là nhịp hiển thị của câu trả lời đã lưu, không
    phải mô hình đang sinh từ. Mọi lượt hỏi đều được ghi lại kèm người hỏi.
  binding:
    kind: MODEL_SEAM
    service: AssistantService
    default_brain: DeterministicAssistantBrain
    why: The disclosure this item exists for. It tells operators the streamed text is display pacing of
      an already-saved answer, not a model generating words. That holds only while AssistantService defaults
      to the deterministic brain. Passing a provider-backed brain to AssistantService(brain=...) is a
      one-line change at a seam that exists so the swap can happen, and it makes the sentence false with
      nothing else in the repository noticing. The test asserts the default.
- slot_id: screens/exceptions.js#hint:fd068299349d
  module: screens/exceptions.js
  key: hint
  text: Cả hai lựa chọn đều không thể rút lại và đều được ghi kèm tên bạn vào nhật ký kiểm toán.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/exceptions.js#guardrail:c542174e4775
  module: screens/exceptions.js
  key: guardrail
  text: Tự động gửi lại một tin không rõ đã đi hay chưa chính là cách khách nhận hai lần cùng một tin
    nhắn. Việc đó bị cấm, không phải là không khuyến khích. Bảng điều khiển này không gửi lại và không
    có nút gửi lại; lối ra duy nhất khỏi trạng thái này là một người có tên xác nhận điều đã thực sự xảy
    ra, bằng cách mở cuộc hội thoại bên phía nhà cung cấp và nhìn tận mắt.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/exceptions.js#notice:d7e95d96d102
  module: screens/exceptions.js
  key: notice
  text: Route trả biên nhận của mọi cửa hàng trong hệ thống, không lọc theo cửa hàng bạn đang chọn ở thanh
    trên. Một biên nhận ở đây có thể thuộc cửa hàng khác, và người bên đó có thể đang xử lý nó cùng lúc
    với bạn — nên hãy đọc lại danh sách trước khi quyết định.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/exceptions.js#notice__title:1571715d5d2f
  module: screens/exceptions.js
  key: notice__title
  text: Bạn đọc được hàng chờ nhưng không quyết định được
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/exceptions.js#screen__lede:172a720934ed
  module: screens/exceptions.js
  key: screen__lede
  text: Hai việc trên màn hình này chạm tới điện thoại của khách. Không có gì ở đây tự gửi, tự gửi lại
    hay tự đóng một trạng thái chưa rõ — mỗi lần đều phải có một người ký tên.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/gaps.js#lede:62259933f062
  module: screens/gaps.js
  key: lede
  text: 'Bảng vận hành hôm nay dừng lại ở chiều thương mại của một đơn: tạo, xác nhận, huỷ. Các chiều
    còn lại của cùng một đơn — nhận đồ, sản xuất, giao, thu tiền — không có bề mặt nào.'
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/gaps.js#missing:c4f99531cea6
  module: screens/gaps.js
  key: missing
  text: 'Không tồn tại aggregate parties, contact_points hay addresses nào. Không có gì để đọc và không
    có gì để ghi. Hệ quả rộng hơn: không có nguồn nào sinh ra bound_contact_id, nên hiện tại hệ thống
    không ghi nhận được khách hàng từ bất kỳ nguồn nào.'
  binding:
    kind: ABSENT_TABLE
    tables:
    - parties
    - contact_points
    - addresses
    why: The party/CRM aggregates. This is the disclosure DEC-015 is about, and it must change the day
      a customer record exists.
- slot_id: screens/gaps.js#today:b318a605cd96
  module: screens/gaps.js
  key: today
  text: Liên hệ được nhận diện phía máy chủ qua contact binding; bảng vận hành không tạo khách.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/gaps.js#missing:6bc3b51a36d1
  module: screens/gaps.js
  key: missing
  text: Các chuyển trạng thái intake không có route HTTP nào. Route /transition chỉ mở ra chiều thương
    mại của đơn, nên IntakeStatus hiển thị được nhưng không đổi được.
  binding:
    kind: ABSENT_ROUTE
    patterns:
    - intake
    why: Claims the intake transitions have no HTTP route. Checked against the generated internal API
      contract rather than the source, so it reads the same governed artifact a reviewer would.
- slot_id: screens/gaps.js#missing:0621c03d85a6
  module: screens/gaps.js
  key: missing
  text: 'Giống hệt intake: trạng thái sản xuất không có route nào. ProductionStatus chỉ đọc được.'
  binding:
    kind: ABSENT_ROUTE
    patterns:
    - production
    why: Same shape for production status. Both become false the day FULFILMENT-001 lands, which is decision-clear
      and unenqueued - exactly when a disclosure needs to notice.
- slot_id: screens/gaps.js#missing:5bb9f3600337
  module: screens/gaps.js
  key: missing
  text: 'Ghi nhận chuyến giao đã có: delivery_legs (DEC-023, 26/08) — ghi đồ đã đến tay khách hay chưa,
    và chuyến trả thành công thì đóng được đơn. Còn thiếu delivery_bundles để gom nhiều đơn vào một chuyến,
    và distance_measurements để lưu quãng đường đã đo — hiện quãng đường do nhân viên nhập từng lần báo
    giá.'
  binding:
    kind: ABSENT_TABLE
    tables:
    - delivery_bundles
    - distance_measurements
    why: The disclosure named three aggregates as absent and the binding asserted the schema lacked them.
      `delivery_legs` landed on 2026-08-26 under DEC-023 and this test failed the same day, which is what
      it is for; the sentence and the binding now name the two that are still absent. Building either
      makes the sentence false again.
- slot_id: screens/gaps.js#missing:9834ff94fcd7
  module: screens/gaps.js
  key: missing
  text: 'Trả đủ đúng số khi khách tự lấy đã có: khối tất toán trong màn hình chi tiết đơn (SETTLEMENT-001,
    đã hoàn thành). Còn thiếu charges, payments và payment_allocations tổng quát, nên trả một phần, trả
    thừa và ghi nợ vẫn không biểu diễn được.'
  binding:
    kind: ABSENT_TABLE
    tables:
    - charges
    - payments
    - payment_allocations
    why: Named literally in the sentence and structurally identical to the four gap notices already bound.
      It was parked DESCRIPTIVE, which is the misclassification this item exists to correct.
- slot_id: screens/gaps.js#missing:af635d639b65
  module: screens/gaps.js
  key: missing
  text: Không có custody_units, custody_events hay batches.
  binding:
    kind: ABSENT_TABLE
    tables:
    - custody_units
    - custody_events
    - batches
    why: 'Same shape: the custody context is measured empty and the disclosure says so.'
- slot_id: screens/gaps.js#missing:4c53a8d9d8b5
  module: screens/gaps.js
  key: missing
  text: Không có remedies, credit_grants hay credit_ledger_entries. Bồi hoàn là một lệnh có duyệt, không
    phải một trạng thái tự do, nên không thể thay bằng một ô ghi chú.
  binding:
    kind: ABSENT_TABLE
    tables:
    - remedies
    - credit_grants
    - credit_ledger_entries
    why: Remedy and credit aggregates. The sentence also states that compensation is an approved command
      rather than a screen action, which stays true while these are absent.
- slot_id: screens/gaps.js#lede:8defef7ab744
  module: screens/gaps.js
  key: lede
  text: Mọi chỉ số vận hành đều cần một read model có phiên bản đứng sau. Chưa có cái nào, nên bảng vận
    hành không hiển thị KPI — đếm tạm vài con số rồi gọi là KPI là cách nhanh nhất để một quyết định kinh
    doanh dựa trên số bịa.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/gaps.js#missing:fadf099317cc
  module: screens/gaps.js
  key: missing
  text: Không có kho dữ liệu nào ghi nhận máy, mẻ giặt hay phút công.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/gaps.js#missing:85d107d2368c
  module: screens/gaps.js
  key: missing
  text: Không có kho dữ liệu nào ghi nhận chi phí của một chuyến giao.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/gaps.js#missing:5a963448822b
  module: screens/gaps.js
  key: missing
  text: Không có read model nào tính funnel, on-time, rewash hay doanh thu. FR-RPT-005 buộc mọi KPI phải
    có tử số, mẫu số, khung thời gian và trạng thái chất lượng dữ liệu — và không có gì cung cấp bốn thứ
    đó.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/gaps.js#today:37f4e66fb029
  module: screens/gaps.js
  key: today
  text: Màn hình Hôm nay chỉ có số bản ghi đang chờ. Đó là số đếm, không phải KPI, và không được đọc như
    KPI.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/gaps.js#missing:bd91192345d1
  module: screens/gaps.js
  key: missing
  text: Không có endpoint xuất dữ liệu nào.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/gaps.js#note:e568b1ae84c4
  module: screens/gaps.js
  key: note
  text: 'Khi làm: CSV phải chống chèn công thức bảng tính — một ô bắt đầu bằng =, +, - hay @ là mã chạy
    được khi khách hàng mở tệp.'
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/gaps.js#missing:105c0b3b7972
  module: screens/gaps.js
  key: missing
  text: Read model đã tồn tại trong kho dữ liệu nhưng chưa có route nào phơi nó ra. Ngoài ra nó cần một
    ProductionSlaPolicy cho mỗi đơn, và đó là một quyết định kinh doanh chứ không phải một mặc định kỹ
    thuật để lấp vào.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/gaps.js#lede:89406ac4c070
  module: screens/gaps.js
  key: lede
  text: Chưa có kênh nào nối vào hệ thống, nên chưa có tin nhắn nào để hợp nhất và chưa có gì để AI tóm
    tắt.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/gaps.js#missing:7d917294ff92
  module: screens/gaps.js
  key: missing
  text: Chưa có channel adapter nào, và chưa có nhà cung cấp nào ở đầu bên kia.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/gaps.js#missing:19ed1c651bfb
  module: screens/gaps.js
  key: missing
  text: Chưa có metric có phiên bản nào để AI trích dẫn.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/gaps.js#note:f427b5746cb8
  module: screens/gaps.js
  key: note
  text: Theo đặc tả, AI chỉ được trích dẫn read model có phiên bản. Nó không được tự tính chỉ số, không
    được sinh SQL, không được chọn định danh và không được thay đổi trạng thái.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/gaps.js#lede:46bbf570458c
  module: screens/gaps.js
  key: lede
  text: Bốn mục dưới đây không thiếu quyết định kinh doanh nào. Chúng thiếu đúng những trường mà API không
    trả về, và một bề mặt đoán bừa các trường đó sẽ là duyệt mù hoặc thao tác nhầm người.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/gaps.js#missing:f1ffb6c9f038
  module: screens/gaps.js
  key: missing
  text: Hàng chờ trả về envelope_hash nhưng không trả resource_version, snapshot_hash hay rendered_hash,
    nên không dựng được một quyết định hợp lệ. Và duyệt theo mã băm mà không xem được nội dung đứng sau
    nó là duyệt mù.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/gaps.js#today:484ca555c5e8
  module: screens/gaps.js
  key: today
  text: Màn hình Duyệt hiển thị hàng chờ và thời gian còn lại; các nút quyết định hiện ra nhưng bị vô
    hiệu hoá kèm lý do.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/gaps.js#missing:8845cac26dd4
  module: screens/gaps.js
  key: missing
  text: Cần một resource_type khớp đúng ánh xạ hành động của máy chủ, cùng hai mã băm JCS và một policy_version.
    Nhân viên không tạo được các giá trị đó bằng tay.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/gaps.js#today:6ba6584bc95b
  module: screens/gaps.js
  key: today
  text: Yêu cầu duyệt do máy chủ tự mở khi một lệnh chạm vào ngưỡng cần duyệt.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/gaps.js#missing:5f121897a2aa
  module: screens/gaps.js
  key: missing
  text: /internal/v1/session không trả session_id, nên bảng vận hành không biết định danh nào để gửi tới
    route thu hồi.
  binding:
    kind: RESPONSE_SHAPE
    module: nha_trang_laundry_api.main
    model: SessionResponse
    absent_field: session_id
    why: 'Claims /internal/v1/session does not return session_id, which is why the operations table cannot
      name a session to revoke. Verified: the model declares staff_user_id, roles and mfa_verified and
      nothing else.'
- slot_id: screens/gaps.js#today:ef2617528ca9
  module: screens/gaps.js
  key: today
  text: Nút Thoát chỉ kết thúc phiên đang dùng. Muốn cắt mọi phiên của một người thì vô hiệu hoá người
    đó ở màn hình Nhân sự — lệnh ấy thu hồi tất cả phiên của họ.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/gaps.js#missing:7569f9e7a62c
  module: screens/gaps.js
  key: missing
  text: Không có route GET cho một revision, và không endpoint nào trả các dòng của báo giá, nên không
    dựng lại được chính thứ vừa tạo ra.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/gaps.js#today:5c44d1dc8bdf
  module: screens/gaps.js
  key: today
  text: Danh sách báo giá của cửa hàng trả về bản mới nhất kèm tổng, trạng thái giá và mã băm ảnh chụp
    — nhưng không có dòng chi tiết nào.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/gaps.js#notice:a7dddda2c256
  module: screens/gaps.js
  key: notice
  text: 'Mỗi mục nêu đúng thứ đang thiếu: một aggregate, một route, một quyết định hoặc một hạng mục công
    việc. Không mục nào hẹn ngày — một lời hẹn ở đây sẽ cũ đi và làm mất tin vào phần còn lại.'
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/incidents.js#notice:bdcdfb355ef5
  module: screens/incidents.js
  key: notice
  text: Mở sự cố chỉ ghi lại rằng có chuyện xảy ra. Máy chủ luôn trả về fault_decided = false và remedy_decided
    = false, và trong API này không có route nào đặt hai giá trị đó. Ai chịu lỗi và khách được bù gì là
    quyết định của con người, ở nơi khác.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/incidents.js#notice__title:2e9baf95d617
  module: screens/incidents.js
  key: notice__title
  text: Ghi nhận sự cố — không phán quyết lỗi, không quyết bồi hoàn
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/incidents.js#hint:adc2a71c0587
  module: screens/incidents.js
  key: hint
  text: 'Luồng duyệt bồi hoàn là một lệnh cần phê duyệt và hiện chưa tồn tại:'
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/incidents.js#notice__title:a726e37ae527
  module: screens/incidents.js
  key: notice__title
  text: Mọi sự cố mở ở đây đều được ghi giống nhau
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/incidents.js#notice__title:0ad0e597c65c
  module: screens/incidents.js
  key: notice__title
  text: Máy chủ từ chối vì ràng buộc, không phải vì lỗi hệ thống
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/incidents.js#notice:1415424437e8
  module: screens/incidents.js
  key: notice
  text: 'Kết quả được phát lại: cùng khoá thao tác và cùng nội dung đã gửi trước đó. Không có sự cố mới
    nào được tạo.'
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/incidents.js#screen__lede:a4289d8c133d
  module: screens/incidents.js
  key: screen__lede
  text: Ghi lại rằng có chuyện xảy ra, kèm mã băm phạm vi liên hệ và mã băm tóm tắt bằng chứng. Việc quy
    lỗi và bồi hoàn không diễn ra ở đây.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/incidents.js#guardrail:8bba1e7a67ac
  module: screens/incidents.js
  key: guardrail
  text: Sự cố mở ở đây luôn là SERVICE_QUALITY do nhân viên ghi, luôn ở trạng thái OPEN, và luôn chưa
    quyết định lỗi lẫn bồi hoàn. Không có route nào trong API này thay đổi ba điều đó.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/manualSend.js#notice:cb979668590b
  module: screens/manualSend.js
  key: notice
  text: Trạng thái này chỉ nói rằng một người đã chứng thực chính họ gửi tin đó bằng tay. Nó không phải
    bằng chứng nhà cung cấp đã chuyển tin và không phải xác nhận của khách. Không được trả lời khách hay
    ghi vào đơn rằng tin đã tới nơi chỉ vì dòng này tồn tại.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/manualSend.js#notice__title:326d1e02c0d3
  module: screens/manualSend.js
  key: notice__title
  text: MANUAL_SEND_RECORDED không có nghĩa là khách đã nhận
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/manualSend.js#notice:759d8966ea11
  module: screens/manualSend.js
  key: notice
  text: 'Kết quả được phát lại: cùng khoá thao tác và cùng nội dung đã gửi trước đó. Không có bản ghi
    mới nào được tạo.'
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/manualSend.js#hint:3cff2232a609
  module: screens/manualSend.js
  key: hint
  text: 'Bốn giá trị trên không đọc lại được từ bất kỳ đường nào của bảng điều khiển: danh sách duyệt
    chỉ trả phong bì_hash. Xin chúng từ người đã tạo yêu cầu duyệt.'
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/manualSend.js#notice:9b57c14c8b63
  module: screens/manualSend.js
  key: notice
  text: Ba ô đầu đã được điền từ kết quả bước 1. Không sửa chúng bằng tay.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/manualSend.js#guardrail:2758a9929d25
  module: screens/manualSend.js
  key: guardrail
  text: Hai bước bên dưới là cách duy nhất một tin đã duyệt rời khỏi hệ thống hôm nay, và cả hai đều ghi
    tên bạn. Khoá phong bì xong thì tiến trình gửi tự động không còn được phép chạy lệnh gửi đó nữa, nên
    đừng khoá một phong bì bạn không định tự gửi.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/orderDetail.js#guardrail:d58009f5dc6c
  module: screens/orderDetail.js
  key: guardrail
  text: 'Chỉ một trường hợp được hỗ trợ: khách trả đúng tổng đã báo, đủ một lần, tại quầy, và tự lấy đồ
    về. Trả thiếu, trả thừa, đặt cọc, trả góp và ghi nợ đều bị từ chối kèm mã quyết định — chủ tiệm đã
    chốt ngày 18/08/2026 là tạm thời không nhận các hình thức này — không làm tròn và không ghi nhận một
    phần. Bản ghi tất toán không sửa được.'
  binding:
    kind: POLICY_BOUND
    decision: DEC-010
    decision_status: RESOLVED
    why: True because a registered decision says so, not because of a code fact. The recorded status is
      regenerated from context/DECISION_REGISTRY.yaml, so a decision resolving after the sentence was
      written shows up here as a registry diff.
- slot_id: screens/orderDetail.js#hint:402155a555ce
  module: screens/orderDetail.js
  key: hint
  text: Bốn trục chuyển động độc lập với nhau; đừng đọc chúng như một chuỗi tuần tự. Lệnh chuyển trạng
    thái nằm ở bảng đơn, vì lệnh đó cần phiên bản dòng vừa đọc được.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/orderDetail.js#notice:3723193c7b25
  module: screens/orderDetail.js
  key: notice
  text: Mã đơn phải là một UUID. Không có yêu cầu nào được gửi đi.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/orderDetail.js#notice__title:42895aeff8a2
  module: screens/orderDetail.js
  key: notice__title
  text: Vai trò này không đọc được dòng thời gian
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/orderDetail.js#notice__title:a904619eb812
  module: screens/orderDetail.js
  key: notice__title
  text: Địa chỉ này không chứa một mã đơn hợp lệ
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/orderDetail.js#eyebrow:069b0c4703fc
  module: screens/orderDetail.js
  key: eyebrow
  text: Ghép từ bảng đơn · máy chủ chưa có cách đọc một đơn riêng lẻ
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/orderDetail.js#guardrail:561748165de4
  module: screens/orderDetail.js
  key: guardrail
  text: Máy chủ chưa có cách đọc một đơn riêng lẻ. Màn hình này gọi lại bảng đơn của cửa hàng và lọc theo
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/orderDetail.js#guardrail:f9986e40a9fa
  module: screens/orderDetail.js
  key: guardrail
  text: Đây là bản ghi kiểm toán theo định danh, không phải lịch sử đơn hàng được biên tập. Thứ tự do
    máy chủ quyết định (occurred_at, id) — cũ nhất trước — và màn hình này không sắp xếp lại. Định danh
    không tồn tại cũng trả về danh sách rỗng chứ không phải lỗi 404.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/orderDetail.js#hint:3ab4a35e058e
  module: screens/orderDetail.js
  key: hint
  text: Tác nhân không phải STAFF — OUTBOX_WORKER, AGENT_RUNNER, BOOTSTRAP — không gắn với người dùng
    nào, nên cột định danh người thực hiện để trống.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/orderRequests.js#notice:258eea73149a
  module: screens/orderRequests.js
  key: notice
  text: Lượt tiếp nhận này đã được ghi trước đó — đây là kết quả cũ hiện lại. Không có yêu cầu mới nào
    được tạo.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/orderRequests.js#notice:0cb2a0fd9969
  module: screens/orderRequests.js
  key: notice
  text: Chỉ cần khi bạn báo lỗi cho kỹ thuật. Thao tác thường ngày không dùng tới nó — nút “Báo giá ngay”
    ở trên đã mang sẵn mã này sang màn hình Báo giá.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/orderRequests.js#screen__lede:a6f669658a61
  module: screens/orderRequests.js
  key: screen__lede
  text: Ghi nhận khách đang ở quầy, gắn với một liên hệ máy chủ đã biết. Bước tiếp theo là báo giá.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/orderRequests.js#guardrail:ea2ce1bbb4e5
  module: screens/orderRequests.js
  key: guardrail
  text: 'Bước này chỉ mở một phiếu nháp: chưa có giá, chưa hẹn giờ, chưa hứa gì với khách. Giá tính ở
    màn hình Báo giá và do máy chủ quyết.'
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/orders.js#notice:9e5d6a7e2900
  module: screens/orders.js
  key: notice
  text: Lệnh này đã chạy trước đó — đây là kết quả cũ hiện lại. Không có bản ghi mới nào được tạo.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/orders.js#notice:81fcdba8cd13
  module: screens/orders.js
  key: notice
  text: Vì máy chủ chưa lưu những thứ đó cho một đơn. Không phải màn hình này giấu đi — chúng chưa tồn
    tại. Hiện một cột “Khách” lấy từ chỗ khác là bịa ra một điều máy chủ chưa từng nói, nên bảng để trống
    và ghi nhận đây là việc chưa làm được.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/orders.js#screen__lede:9c4d5677d901
  module: screens/orders.js
  key: screen__lede
  text: Đơn nào đang ở đâu. Mỗi đơn có bốn phần chạy riêng — bán hàng, nhận đồ, làm đồ, tiền — nên hãy
    đọc từng phần một.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/orders.js#guardrail:fbf408806822
  module: screens/orders.js
  key: guardrail
  text: Bốn phần của một đơn — bán hàng, nhận đồ, làm đồ, tiền — chạy riêng với nhau. Đơn đã xác nhận
    vẫn có thể chưa bắt đầu giặt và chưa thu tiền. Đọc bốn nhãn như bốn câu trả lời riêng, không phải
    một chuỗi nối tiếp.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/orders.js#guardrail:06a4b6c85d26
  module: screens/orders.js
  key: guardrail
  text: Màn hình này cố ý không biết bước nào là hợp lệ — quy tắc đó thuộc về máy chủ, và chép nó sang
    trình duyệt là tạo bản thứ hai không ai giữ cho khớp được. Cả tám đích đều được chào; máy chủ từ chối
    cái nào không hợp lệ và nói rõ vướng ở đâu.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/orders.js#guardrail:e14089d6d5db
  module: screens/orders.js
  key: guardrail
  text: Khách đã trả đủ tại quầy trước khi đồ rời tiệm, nên ghi nhận ở đây không có tiền — chỉ ghi đồ
    đã đến tay khách hay chưa. Giao hụt thì ghi thất bại, không tính thêm phí, và lần giao sau là một
    dòng mới. Chỉ chuyến TRẢ ĐỒ thành công mới cho phép đóng đơn.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/orders.js#guardrail:a0b69d4833f1
  module: screens/orders.js
  key: guardrail
  text: Đơn chỉ được tạo từ một báo giá khách đã chốt. Bấm \u201cKhách đã chốt giá\u201d ở màn hình Báo
    giá trước, rồi mới tạo đơn ở đây. Máy chủ kiểm lại toàn bộ điều kiện; màn hình này chỉ bắt lỗi gõ
    trước khi gửi.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/quotes.js#hint:2aceac798e0c
  module: screens/quotes.js
  key: hint
  text: Đã chốt. Bản này không sửa được nữa, và người chốt đã được ghi lại.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/quotes.js#hint:20b051f8212b
  module: screens/quotes.js
  key: hint
  text: Bấm khi khách đã nghe giá và đồng ý. Tên bạn sẽ được ghi lại.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/quotes.js#notice:759d8966ea11
  module: screens/quotes.js
  key: notice
  text: 'Kết quả được phát lại: cùng khoá thao tác và cùng nội dung đã gửi trước đó. Không có bản ghi
    mới nào được tạo.'
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/quotes.js#notice:138760e6c1ca
  module: screens/quotes.js
  key: notice
  text: Máy chủ trả về một bảng giá rỗng, nên không có dịch vụ nào để chọn và không thể tính giá. Đây
    là vấn đề của bảng giá đã công bố, không phải của thao tác này.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/quotes.js#notice:be0adcdc34b4
  module: screens/quotes.js
  key: notice
  text: Chưa có lượt tiếp nhận nào trong cửa hàng này. Tạo một lượt ở màn hình
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/quotes.js#notice:7c81144140bd
  module: screens/quotes.js
  key: notice
  text: Không có dữ kiện nào được điền sẵn; hãy chọn từ danh sách bên dưới.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/quotes.js#notice:580264d6aa9f
  module: screens/quotes.js
  key: notice
  text: Mã có thể thuộc cửa hàng khác hoặc đã bị gõ sai — máy chủ trả lời cùng một cách cho cả hai, nên
    màn hình này cũng không đoán. Không có dữ kiện nào được điền sẵn; hãy chọn từ danh sách bên dưới.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/quotes.js#notice__title:037d161023b5
  module: screens/quotes.js
  key: notice__title
  text: Không tìm thấy yêu cầu này trong cửa hàng đang chọn
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/quotes.js#screen__lede:bd8e99192039
  module: screens/quotes.js
  key: screen__lede
  text: Chọn yêu cầu, chọn dịch vụ, nhập khối lượng — máy chủ tính theo bảng giá đã chốt. Màn hình này
    không tự cộng tiền, không làm tròn.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/quotes.js#guardrail:473650fae3a4
  module: screens/quotes.js
  key: guardrail
  text: Thiếu dữ kiện thì máy chủ từ chối đoán và không ghi gì cả — người quyết, không phải máy.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/shadow.js#eyebrow:74a8d714980a
  module: screens/shadow.js
  key: eyebrow
  text: Nội dung bản nháp · Văn bản không tin cậy
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/shadow.js#hint:b22af4ed5982
  module: screens/shadow.js
  key: hint
  text: Bản gốc của agent vẫn nằm nguyên trong sổ. Dòng này là quyết định của bạn, không phải một tin
    nhắn đã gửi đi.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/shadow.js#notice:f0ce709a4a3f
  module: screens/shadow.js
  key: notice
  text: Hàng chờ này chỉ hiện những bản nháp chưa ai quyết định. Nếu bản nháp biến mất sau khi tải lại,
    đã có một quyết định được ghi cho lượt chạy đó. Nếu nó vẫn còn sau khi bạn tải lại cả màn hình, chưa
    có gì được ghi và bạn quyết định lại được.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/shadow.js#notice__title:a7adeba96cad
  module: screens/shadow.js
  key: notice__title
  text: Cách biết chắc quyết định có được ghi hay không
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/shadow.js#notice:7f035d90f7b2
  module: screens/shadow.js
  key: notice
  text: Mỗi tin nhắn gửi ra ngoài đều cần một người có tên quyết định. Hệ thống không tự gửi bất cứ thứ
    gì, và bấm APPROVE ở đây cũng chưa gửi gì cả — nó chỉ ghi rằng bạn đồng ý với nội dung.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/shadow.js#screen__lede:efe092e0bc31
  module: screens/shadow.js
  key: screen__lede
  text: Mọi thứ agent soạn ra dừng lại ở đây. Màn hình này ghi lại quyết định của bạn về từng bản nháp;
    nó không gửi tin nhắn nào cho khách.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/shadow.js#guardrail:add42213ce89
  module: screens/shadow.js
  key: guardrail
  text: Duyệt ở đây không phải là gửi. Một bản nháp rời khỏi hàng chờ này ngay khi có người quyết định,
    và mỗi lượt chạy chỉ nhận đúng một quyết định.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/shadow.js#guardrail:4202f9908977
  module: screens/shadow.js
  key: guardrail
  text: Đây là những bản nháp đã có người quyết định. Bản gốc của agent nằm cạnh quyết định để sau này
    chấm được agent bằng việc thật. Không dòng nào ở đây là một tin nhắn đã gửi đi.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/staff.js#guardrail:5a17bb3bc1fc
  module: screens/staff.js
  key: guardrail
  text: Tạo xong là có danh tính, chưa có vai trò và chưa có cửa hàng. Người này chưa làm được gì cho
    tới khi được gán vai trò và được gán cửa hàng — cả hai việc đó làm ngay trên màn hình này, ở các bảng
    Gán vai trò và Gán cửa hàng bên dưới, và chỉ chủ mới thực hiện được.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/staff.js#hint:73bbac9c3756
  module: screens/staff.js
  key: hint
  text: Chép lại mã này. Máy chủ chưa có cách liệt kê hay tra cứu nhân sự, nên nếu mất mã thì hai lệnh
    bên dưới không dùng được cho người này nữa.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/staff.js#notice:14c7ff28b9f3
  module: screens/staff.js
  key: notice
  text: Máy chủ chưa có cách đọc lại vai trò của một nhân viên, nên bảng vận hành không thể xác nhận danh
    sách vai trò hiện tại của người này. Chỉ biết lệnh vừa rồi đã được nhận.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/staff.js#guardrail:304eee7a5f54
  module: screens/staff.js
  key: guardrail
  text: 'Gán vai trò là cộng thêm, không phải thay thế: lệnh này không gỡ vai trò nào đang có. Bảng vận
    hành không đọc được vai trò hiện có và máy chủ chưa có cách thu hồi vai trò, nên hãy chắc chắn trước
    khi gửi.'
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/staff.js#notice:fbe05be31e9a
  module: screens/staff.js
  key: notice
  text: Máy chủ chưa có cách bật lại một nhân viên đã vô hiệu hoá. Nếu cần người này làm việc lại, phải
    xử lý trực tiếp ở cơ sở dữ liệu.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/staff.js#notice:aeb3d72b4c8d
  module: screens/staff.js
  key: notice
  text: Người này sẽ bị chuyển sang DISABLED và mọi phiên đang mở của họ bị thu hồi ngay lập tức, kể cả
    khi đang đứng ở quầy. Máy chủ từ chối nếu đây là chủ đang hoạt động cuối cùng.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/staff.js#guardrail:7fd8cda5fa06
  module: screens/staff.js
  key: guardrail
  text: Lệnh này cũng thu hồi toàn bộ phiên đăng nhập của người đó trong cùng một giao dịch. Không có
    đường quay lại từ bảng vận hành.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/staff.js#notice:f78020c49fd6
  module: screens/staff.js
  key: notice
  text: Máy chủ chưa có cách đọc lại danh sách cửa hàng của người khác, nên bảng vận hành không xác nhận
    được trạng thái hiện tại của họ. Người đó đăng nhập rồi xem thanh trên cùng là cách kiểm tra chắc
    chắn.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/staff.js#guardrail:964c82c63c46
  module: screens/staff.js
  key: guardrail
  text: Không có dòng gán thì mọi màn hình theo cửa hàng đều từ chối người đó, kể cả khi vai trò đã đúng.
    Thu hồi có hiệu lực ngay ở yêu cầu kế tiếp; dòng gán vẫn được giữ lại để biết ai đã cấp và ai đã thu
    hồi.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/staff.js#screen__lede:cc74d8be4eaa
  module: screens/staff.js
  key: screen__lede
  text: 'Ba lệnh danh tính mà máy chủ có. Máy chủ chưa có cách đọc lại nhân sự, nên màn hình này viết
    chứ không đọc: mọi thứ nó khẳng định là kết quả của lệnh vừa gửi, không phải trạng thái đang có.'
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/system.js#hint:ff4da365acef
  module: screens/system.js
  key: hint
  text: Sự kiện đã ghi cùng giao dịch nghiệp vụ và đang chờ tiến trình gửi phát đi. Đây là nơi một lệnh
    đã được lưu nhưng chưa rời khỏi hệ thống.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/system.js#hint:4a7024c4b8cf
  module: screens/system.js
  key: hint
  text: Lượt chạy của agent. Một lượt chạy chỉ soạn bản nháp; nó không gửi cho khách và không quyết định
    tiền, nên tồn đọng ở đây làm chậm việc, không làm sai tiền.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/system.js#notice__title:174866b3b1cb
  module: screens/system.js
  key: notice__title
  text: replay_available — không dùng để quyết định gì
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/system.js#hint:289c7ed81944
  module: screens/system.js
  key: hint
  text: Cửa hàng đang chọn không ảnh hưởng tới các con số phía trên; hàng đợi được đếm trên toàn hệ thống.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/system.js#notice:d6ee2eb7a0df
  module: screens/system.js
  key: notice
  text: Máy chủ chỉ trả về mã nhân viên, vai trò và trạng thái xác thực hai bước cho phiên đang dùng;
    nó không trả về mã phiên. Thu hồi một phiên cần đúng mã phiên đó, nên bảng vận hành không thể liệt
    kê các phiên đang mở, cũng không thể đóng phiên trên máy khác. Nút “Thoát” trên thanh phía trên chỉ
    kết thúc phiên này.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/system.js#notice__title:00d1c77e25cb
  module: screens/system.js
  key: notice__title
  text: Không liệt kê và không thu hồi được phiên khác từ đây
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/system.js#hint:f3257e638ddc
  module: screens/system.js
  key: hint
  text: Số quá hạn khóa tạm nằm bên trong số PROCESSING, không phải một nhóm riêng. Đừng cộng bốn dòng
    lại với nhau — màn hình này cố ý không hiển thị tổng.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/system.js#screen__lede:c7d1d984335f
  module: screens/system.js
  key: screen__lede
  text: Độ sâu của hai hàng đợi chạy phía sau bảng vận hành, đọc trực tiếp từ cơ sở dữ liệu tại thời điểm
    bạn tải màn hình.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/system.js#guardrail:cb8f55ac1fcf
  module: screens/system.js
  key: guardrail
  text: Màn hình này chỉ để nhìn. Không phát lại, không thử lại, không xếp lại — và các con số đếm trên
    mọi cửa hàng của hệ thống, không theo cửa hàng đang chọn.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/today.js#notice:520eb5e2587a
  module: screens/today.js
  key: notice
  text: Không đếm được, và hiển thị số 0 ở đây sẽ là sai sự thật. Báo cho kỹ thuật.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/today.js#notice__title:0bbbb7536fca
  module: screens/today.js
  key: notice__title
  text: Máy chủ trả về hình dạng lạ cho danh sách này
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/today.js#hint:1149773bc2f0
  module: screens/today.js
  key: hint
  text: Chọn một cửa hàng để xem số tiền đã thu.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: screens/today.js#screen__lede:80fea583bc59
  module: screens/today.js
  key: screen__lede
  text: Việc đang chờ bạn quyết định và tình hình cửa hàng — chỉ những gì cần một người xử lý.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: ui/components.js#notice:b2539cf37219
  module: ui/components.js
  key: notice
  text: 'Dưới 6kg và từ 6kg trở lên là hai bậc đơn giá khác nhau, và bậc dưới tính tối thiểu 1kg. Hệ quả:
    một đơn 5,9kg có thể đắt hơn một đơn 6,0kg. Đây là quy tắc chủ đã xác nhận, không phải lỗi, và không
    được làm trơn.'
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: ui/components.js#hint:5e24fe1227d6
  module: ui/components.js
  key: hint
  text: Không tách bao hay tách dòng để lách bậc giá. Số tiền do máy chủ tính; màn hình này không tính.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: ui/components.js#notice:9525c4bc0839
  module: ui/components.js
  key: notice
  text: Không có API phía sau. Hiển thị một biểu mẫu ở đây sẽ tạo cảm giác dữ liệu đã được lưu
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
- slot_id: ui/components.js#notice:f34c6be68acb
  module: ui/components.js
  key: notice
  text: Không có API phía sau. Hiển thị một biểu mẫu ở đây sẽ tạo cảm giác dữ liệu đã được lưu trong khi
    không có gì được lưu cả.
  binding:
    kind: DESCRIPTIVE
    why: Prose about how this screen behaves. No single code fact governs it; it is registered so that
      rewording it fails the check and forces a fresh reading.
